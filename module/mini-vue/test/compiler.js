require('mocha')
require('should')

// const htmlParser = require('../../../build/module/mini-vue/compiler/htmlParser')
// const templateParser = require('../../../build/module/mini-vue/compiler/templateParser')
// const optimizer = require('../../../build/module/mini-vue/compiler/optimizer')

// const template = `
// <div class="counter">
//   <p>Count result is: {{ count }}</p>
//   <button :disable="count === 3">+</button>
//   <button :disable="count === 0">-</button>
//   <p>Click the buttons to add or dec the count number</p>
// </div>
// <p>Hello, here's an test line.</p>
// <p>{{ count ? 'yes' : 'no' }}</p>
// `

// const ast = optimizer(htmlParser(template, { text: templateParser }))

// console.log(ast)

// describe('Parse text to expression works fine', function () {

// })
